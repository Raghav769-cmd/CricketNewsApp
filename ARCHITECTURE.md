# Authentication System Architecture

## 🔄 User Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                        REGISTRATION FLOW                             │
└─────────────────────────────────────────────────────────────────────┘

User Input: Email, Password, Role
    ↓
Frontend Validation
    ↓
POST /api/auth/register
    ↓
Backend Validation (email unique, password strength)
    ↓
bcryptjs.hash(password, 10)  ← Hash password
    ↓
INSERT INTO users (email, password_hash, role)
    ↓
Generate JWT Token (7 day expiration)
    ↓
Return { token, user } to frontend
    ↓
localStorage.setItem('authToken', token)
localStorage.setItem('authUser', user)
    ↓
Redirect to /matches
```

## 🔐 Login Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                          LOGIN FLOW                                  │
└─────────────────────────────────────────────────────────────────────┘

User Input: Email, Password
    ↓
POST /api/auth/login
    ↓
SELECT * FROM users WHERE email = $1
    ↓
bcryptjs.compare(inputPassword, storedHash)
    ↓
Is Match?
├─ NO  → Return 401 Unauthorized
│
└─ YES → Generate JWT Token
         ↓
         jwt.sign({ id, email, role }, JWT_SECRET, { expiresIn: '7d' })
         ↓
         Return { token, user }
         ↓
         localStorage.setItem('authToken', token)
         localStorage.setItem('authUser', user)
         ↓
         Redirect to /matches
```

## 🛡️ Protected Request Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PROTECTED REQUEST FLOW                            │
└─────────────────────────────────────────────────────────────────────┘

Frontend Component
    ↓
const { token } = useAuth()
    ↓
fetch(url, {
  headers: {
    'Authorization': `Bearer ${token}`
  }
})
    ↓
Backend Middleware: verifyToken
    ├─ Extract token from "Authorization: Bearer {token}"
    ├─ jwt.verify(token, JWT_SECRET)
    ├─ Decode: { id, email, role, iat, exp }
    └─ req.user = decoded
    ↓
Is Token Valid?
├─ NO  → Return 401 Unauthorized
│        { error: 'Invalid token' }
│
└─ YES → Check Authorization (if admin-only route)
         ├─ req.user.role === 'admin'?
         ├─ NO  → Return 403 Forbidden
         │        { error: 'Only admins can perform this action' }
         │
         └─ YES → Execute Route Handler
                  ↓
                  Return Response (200, 201, etc.)
```

## 🏗️ System Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (Next.js)                           │
├──────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                    React Components                             │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ • Login Page          → useAuth()                              │  │
│  │ • Register Page       → useAuth()                              │  │
│  │ • Matches Page        → useAuth()                              │  │
│  │ • Header Component    → useAuth()                              │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                    ↑                                   │
│                         useAuth() Hook                                 │
│                                    ↑                                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │         AuthContext (React Context API)                         │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ • user: User | null                                             │  │
│  │ • token: string | null                                          │  │
│  │ • isAuthenticated: boolean                                      │  │
│  │ • isAdmin: boolean                                              │  │
│  │ • login(email, password)                                        │  │
│  │ • register(email, password, role)                               │  │
│  │ • logout()                                                      │  │
│  │ • loading: boolean                                              │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                   │
│                  localStorage (Token Persistence)                     │
│                                                                        │
└──────────────────────────────────────────────────────────────────────┘
                                    ↕
                          HTTP + JWT Token
                    Authorization: Bearer {token}
                                    ↕
┌──────────────────────────────────────────────────────────────────────┐
│                         BACKEND (Express)                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                      Route Handlers                             │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ POST   /api/auth/register                                      │  │
│  │ POST   /api/auth/login                                         │  │
│  │ GET    /api/auth/me          [verifyToken]                     │  │
│  │ POST   /api/auth/logout      [verifyToken]                     │  │
│  │ POST   /api/matches          [verifyToken, isAdmin]            │  │
│  │ DELETE /api/matches/:id      [verifyToken, isAdmin]            │  │
│  │ GET    /api/matches          [Public]                          │  │
│  │ GET    /api/matches/:id      [Public]                          │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                    ↑                                   │
│                         Middleware Pipeline                           │
│                                    ↑                                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │              Authentication Middleware                          │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ • verifyToken()    → Check & decode JWT                        │  │
│  │ • isAdmin()        → Check user.role === 'admin'               │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │            Authentication Logic (auth.ts)                       │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ • generateToken(id, email, role)                               │  │
│  │ • verifyToken(token)                                           │  │
│  │ • hashPassword(password)                                       │  │
│  │ • comparePassword(plain, hash)                                 │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                    ↓                                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                   Database (PostgreSQL)                         │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ ┌────────────────────────────────────────────────────────────┐ │  │
│  │ │         users TABLE                                        │ │  │
│  │ ├────────────────────────────────────────────────────────────┤ │  │
│  │ │ id (PK)     │ email (UNIQUE) │ password_hash │ role │ ts  │ │  │
│  │ ├────────────────────────────────────────────────────────────┤ │  │
│  │ │ 1           │ admin@...      │ $2a$10$...    │admin │ ...│ │  │
│  │ │ 2           │ user@...       │ $2a$10$...    │user  │ ...│ │  │
│  │ └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │ ┌────────────────────────────────────────────────────────────┐ │  │
│  │ │      Other tables (matches, teams, players, etc.)         │ │  │
│  │ └────────────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                        │
└──────────────────────────────────────────────────────────────────────┘
```

## 📊 JWT Token Structure

```
JWT Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwiZW1haWwiOiJhZG1pbkBjcmlja2V0LmNvbSIsInJvbGUiOiJhZG1pbiIsImlhdCI6MTczMzM2NTAwMCwiZXhwIjoxNzM0NjAxMjAwfQ.xyz...

├─ Header
│  └─ { "alg": "HS256", "typ": "JWT" }
│
├─ Payload
│  ├─ "id": 1
│  ├─ "email": "admin@cricket.com"
│  ├─ "role": "admin"
│  ├─ "iat": 1733365000 (issued at)
│  └─ "exp": 1734601200 (expires - 7 days later)
│
└─ Signature
   └─ HMACSHA256(header.payload, JWT_SECRET)
```

## 🔐 Role-Based Access Control (RBAC)

```
┌────────────────────────────────────────────────────────────────┐
│                    AUTHORIZATION MATRIX                         │
├────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Endpoint              │  Public  │  Admin  │  User              │
│  ─────────────────────────────────────────────────────────────  │
│  GET /api/matches      │    ✅    │   ✅    │   ✅               │
│  GET /api/matches/:id  │    ✅    │   ✅    │   ✅               │
│  POST /api/matches     │    ❌    │   ✅    │   ❌               │
│  DELETE /api/matches   │    ❌    │   ✅    │   ❌               │
│  GET /api/auth/me      │    ❌    │   ✅    │   ✅               │
│  POST /api/auth/logout │    ❌    │   ✅    │   ✅               │
│                                                                  │
└────────────────────────────────────────────────────────────────┘

Legend:
  ✅ = Allowed
  ❌ = Denied (returns 403 Forbidden)
  Public = No token required
```

## 🔄 State Diagram

```
                    ┌─────────────────────┐
                    │   NOT AUTHENTICATED │
                    └─────────┬───────────┘
                              │
                    Register / Login
                              │
                              ↓
                    ┌─────────────────────┐
         ┌──────────→│  AUTHENTICATED     │←─────────────┐
         │           │ Token in storage   │              │
         │           └─────────┬──────────┘              │
         │                     │                         │
         │         Try to use app                        │
         │                     │                         │
         │                     ↓                         │
         │         ┌─────────────────────┐              │
         │         │  Check Token Valid? │              │
         │         └──┬─────────────────┬─┘              │
         │            │                 │                │
         │         YES│                 │NO              │
         │            │                 │                │
         │    ┌───────┘                 └────────┐       │
         │    ↓                                  ↓       │
         │ ┌──────────────┐          ┌──────────────────┐
         │ │ Normal Flow  │          │ Show Login Page  │
         │ │ (Call API)   │          │ Clear Storage    │
         │ │              │          │                  │
         │ └──────────────┘          └──────────────────┘
         │                                      │
         └──────────────────────────────────────┘
                     Logout
```

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: PASSWORD HASHING (Backend)                        │
│  ├─ Input: plain password                                   │
│  ├─ Process: bcryptjs (10 salt rounds)                      │
│  └─ Output: $2a$10$NLnnUzEk8sxF/7sBx0UJ2eYPZVUHmqkiNZ2...  │
│                                                              │
│  Layer 2: JWT SIGNING (Backend)                             │
│  ├─ Input: { id, email, role }                              │
│  ├─ Process: HMACSHA256(header.payload, JWT_SECRET)         │
│  └─ Output: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...          │
│                                                              │
│  Layer 3: TOKEN TRANSMISSION (Frontend → Backend)           │
│  ├─ Should use: HTTPS (not HTTP)                            │
│  ├─ Header: Authorization: Bearer {token}                   │
│  └─ Never: In URL, in cookies (for now), in body            │
│                                                              │
│  Layer 4: TOKEN VERIFICATION (Backend)                      │
│  ├─ Extract token from header                               │
│  ├─ Verify signature with JWT_SECRET                        │
│  ├─ Check expiration time                                   │
│  └─ Attach decoded user to request object                   │
│                                                              │
│  Layer 5: AUTHORIZATION CHECK (Backend)                     │
│  ├─ Verify user is authenticated                            │
│  ├─ Check user role for endpoint                            │
│  ├─ Enforce role-based access                               │
│  └─ Return 403 if insufficient permissions                  │
│                                                              │
│  Layer 6: TOKEN STORAGE (Frontend)                          │
│  ├─ Current: localStorage                                   │
│  ├─ Future: httpOnly cookies (recommended)                  │
│  └─ Logout: Clear storage                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📈 Data Flow Diagram

```
USER REGISTRATION:
┌─────────┐     ┌──────────────┐     ┌────────────┐     ┌──────────┐
│ Browser │────→│ Express API  │────→│  bcryptjs  │────→│ Database │
│         │     │              │     │            │     │          │
│ POST    │     │ Route        │     │ Hash       │     │ INSERT   │
│ /auth/  │     │ Handler      │     │ password   │     │ INTO     │
│ register│     │              │     │            │     │ users    │
└────┬────┘     └──────┬───────┘     └────┬───────┘     └──────────┘
     │                 │                   │
     │                 ↓                   │
     │          ┌──────────────┐           │
     │          │  Generate    │           │
     │          │  JWT Token   │           │
     │          └──────┬───────┘           │
     │                 ↓                   │
     │          ┌──────────────┐           │
     ←──────────│ Return       │←──────────┘
               │ token +user  │
               └──────────────┘
                     ↓
               localStorage
                     ↓
               redirect /matches


PROTECTED REQUEST:
┌─────────┐     ┌──────────────┐     ┌────────────┐     ┌──────────┐
│ Browser │────→│ Express API  │────→│ Verify JWT │────→│ Database │
│         │     │              │     │            │     │          │
│ GET     │     │ Route        │     │ Check      │     │ SELECT * │
│ /matches│     │ Handler      │     │ expiration │     │ FROM     │
│         │     │              │     │            │     │ matches  │
└─────────┘     └──────┬───────┘     └──────┬─────┘     └──────────┘
                       │                    │
                       │    Token Valid?    │
                       │                    ↓
                       │              Token: YES ✅
                       │                    │
                       │                    ↓
                       │           Check: Is Admin?
                       │                    │
                       │          (Not needed for GET)
                       │                    │
                       ← Response Data ←────┘
```

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Layered security approach
- ✅ Type-safe authentication
- ✅ Scalable permission model
- ✅ Easy to audit and maintain
